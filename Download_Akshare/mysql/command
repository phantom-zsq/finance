# 导出整个数据库,不包含数据
mysqldump -u root -p -d akshare > akshare_no_data.sql
# 导出整个数据库,包含数据
mysqldump -u root -p akshare > akshare.sql
#手动插入数据
#手动插入涨跌幅数据
INSERT INTO futures_rule
SELECT
    交易所,
	品种,
	代码,
	交易保证金比例,
	涨跌停板幅度,
	合约乘数,
	最小变动价位,
	限价单每笔最大下单手数,
	特殊合约参数调整,
	调整备注,
	'20260807' AS 交易日
FROM futures_rule
WHERE 交易日='20260806';